# 📱 مشروع Getir Clone - نظرة شاملة على المشروع

## 🎯 نظرة عامة على المشروع

**Getir Clone** هو تطبيق ويب متكامل لتوصيل البقالة والمنتجات الأساسية في غضون 10 دقائق، مستوحى من تطبيق Getir الشهير. المشروع مبني باستخدام Laravel 12 مع React وInertia.js، ويوفر تجربة تسوق سريعة ومريحة للعملاء.

### 🚀 الميزات الرئيسية

- **توصيل سريع**: توصيل في 10 دقائق فقط
- **متاجر متعددة**: شبكة من المتاجر المنتشرة في المدينة
- **منتجات متنوعة**: آلاف المنتجات عبر فئات مختلفة
- **نظام دفع متقدم**: دفع نقدي، بالبطاقة، ومحفظة إلكترونية
- **تتبع الطلبات**: متابعة الطلب في الوقت الفعلي
- **واجهات متعددة**: للعملاء، أصحاب المتاجر، السائقين، والإدارة

---

## 💳 طرق الدفع المتاحة

يدعم النظام ثلاث طرق دفع رئيسية:

### 1. 💰 الدفع النقدي (Cash)
- **الطريقة الافتراضية** في النظام
- الدفع عند الاستلام
- مناسب للعملاء الذين يفضلون الدفع التقليدي

### 2. 💳 الدفع بالبطاقة (Card)
- بطاقات ائتمانية ودباييت
- معالجة آمنة للمدفوعات
- تشفير متقدم لحماية البيانات

### 3. 📱 المحفظة الإلكترونية (Wallet)
- رصيد مسبق في حساب العميل
- معاملات سريعة وآمنة
- إدارة الرصيد من خلال التطبيق

### 🔒 أمان المدفوعات
- تشفير SSL/TLS لجميع المعاملات
- عدم تخزين تفاصيل البطاقات على الخوادم
- معالجات دفع آمنة ومعتمدة
- ضمان استرداد 100% في حالة عدم الرضا

---

## 🚚 نظام التوصيل والعاملين

### 📍 نظام التوصيل الجغرافي

#### **تحديد المتجر الأقرب**
- النظام يحدد تلقائياً أقرب متجر للموقع
- حساب المسافة باستخدام الإحداثيات الجغرافية
- دائرة توصيل محددة لكل متجر (افتراضياً 5 كم)

#### **إدارة مناطق التوصيل**
```php
// مثال على حساب المسافة
private function calculateDistance($latitude, $longitude)
{
    $earthRadius = 6371; // نصف قطر الأرض بالكيلومتر
    
    $latDiff = deg2rad($latitude - $this->latitude);
    $lonDiff = deg2rad($longitude - $this->longitude);
    
    $a = sin($latDiff / 2) * sin($latDiff / 2) +
         cos(deg2rad($this->latitude)) * cos(deg2rad($latitude)) *
         sin($lonDiff / 2) * sin($lonDiff / 2);
    
    $c = 2 * atan2(sqrt($a), sqrt(1 - $a));
    
    return $earthRadius * $c;
}
```

### 🚗 إدارة سائقي التوصيل

#### **أنواع المركبات المدعومة**
- 🏍️ **دراجة نارية** (افتراضي)
- 🚲 **دراجة هوائية**
- 🚗 **سيارة**

#### **حالات السائق**
- **متاح (Available)**: جاهز لتلقي طلبات جديدة
- **مشغول (Busy)**: يقوم بتوصيل طلب حالياً
- **غير متصل (Offline)**: خارج الخدمة

#### **تتبع الموقع**
- تحديث الموقع في الوقت الفعلي
- تتبع مسار التوصيل
- حساب وقت الوصول المتوقع

#### **نظام التقييم**
- تقييم العملاء للسائقين (1-5 نجوم)
- إحصائيات التوصيل
- أداء السائق وإنجازاته

### ⏱️ أوقات التوصيل

#### **التوقيت المتوقع**
- **افتراضي**: 10 دقائق
- **قابل للتخصيص** لكل متجر
- **حساب ديناميكي** حسب المسافة والمرور

#### **مراحل التوصيل**
1. **انتظار (Pending)**: الطلب في الانتظار
2. **مؤكد (Confirmed)**: تم تأكيد الطلب
3. **تحضير (Preparing)**: يتم تحضير الطلب
4. **جاهز (Ready)**: الطلب جاهز للتوصيل
5. **في الطريق (Out for Delivery)**: السائق في الطريق
6. **تم التوصيل (Delivered)**: تم التسليم بنجاح

---

## 👥 أدوار المستخدمين

### 🛒 العميل (Customer)

#### **الميزات المتاحة**
- تصفح المنتجات والفئات
- إضافة المنتجات للسلة
- إتمام الطلب والدفع
- تتبع حالة الطلب
- تقييم المنتجات والسائقين
- إدارة الملف الشخصي
- حفظ العناوين المفضلة

#### **عملية التسوق**
1. **التسجيل**: رقم الهاتف + رمز التحقق
2. **التصفح**: فئات ومنتجات متنوعة
3. **السلة**: إضافة/إزالة المنتجات
4. **الدفع**: اختيار طريقة الدفع
5. **المتابعة**: تتبع الطلب حتى التوصيل

#### **نظام التحقق**
```php
// مثال على التحقق من الهاتف
public function verifyCode($code)
{
    if ($this->verification_code === $code && 
        $this->verification_code_expires_at && 
        $this->verification_code_expires_at->isFuture()) {
        
        $this->is_verified = true;
        $this->verification_code = null;
        $this->verification_code_expires_at = null;
        $this->save();
        
        return true;
    }
    
    return false;
}
```

### 🏪 صاحب المتجر (Store Owner)

#### **لوحة التحكم**
- إحصائيات المبيعات والإيرادات
- إدارة المنتجات والمخزون
- متابعة الطلبات الجديدة
- إحصائيات الأداء اليومي والشهري
- إدارة معلومات المتجر

#### **إحصائيات المتجر**
```php
// مثال على الإحصائيات
$stats = [
    'total_orders' => $store->orders()->count(),
    'pending_orders' => $store->orders()->where('status', 'pending')->count(),
    'preparing_orders' => $store->orders()->where('status', 'preparing')->count(),
    'delivered_orders' => $store->orders()->where('status', 'delivered')->count(),
    'total_revenue' => $store->orders()->where('status', 'delivered')->sum('total_amount'),
    'total_products' => Product::count(),
];
```

#### **إدارة المنتجات**
- إضافة منتجات جديدة
- تحديث الأسعار والكميات
- إدارة الفئات والتصنيفات
- تحليل المنتجات الأكثر طلباً

### 🚚 سائق التوصيل (Driver)

#### **التطبيق المخصص**
- استقبال الطلبات الجديدة
- تحديث حالة الطلب
- تتبع الموقع والمسارات
- التواصل مع العملاء
- إدارة الملف الشخصي والأداء

#### **نظام المهام**
- طلبات متاحة في المنطقة
- تفاصيل العميل والموقع
- تعليمات التوصيل
- تحديث حالة التسليم

### 👨‍💼 الإدمن (Admin)

#### **لوحة التحكم الشاملة**
- إدارة جميع المستخدمين
- مراقبة النظام والأداء
- إحصائيات شاملة
- إدارة المتاجر والمنتجات
- دعم العملاء

#### **إحصائيات النظام**
```php
// مثال على الإحصائيات العامة
$stats = [
    'total_users' => User::count(),
    'total_customers' => User::customers()->count(),
    'total_store_owners' => User::storeOwners()->count(),
    'total_stores' => Store::count(),
    'active_stores' => Store::active()->count(),
    'total_products' => Product::count(),
    'total_orders' => Order::count(),
    'total_revenue' => Order::where('status', 'delivered')->sum('total_amount'),
];
```

---

## 🏗️ البنية التقنية

### 🛠️ التقنيات المستخدمة

#### **Backend**
- **Laravel 12**: إطار عمل PHP متقدم
- **MySQL/SQLite**: قاعدة البيانات
- **Laravel Sanctum**: المصادقة والتفويض
- **Laravel Inertia**: ربط الواجهة الأمامية

#### **Frontend**
- **React**: مكتبة واجهة المستخدم
- **Inertia.js**: ربط Laravel مع React
- **Tailwind CSS**: تصميم الواجهة
- **Vite**: أداة البناء والتطوير

#### **الميزات التقنية**
- **PWA**: تطبيق ويب متقدم
- **Responsive Design**: متوافق مع جميع الأجهزة
- **Real-time Updates**: تحديثات فورية
- **Multi-language**: دعم العربية والإنجليزية

### 📊 قاعدة البيانات

#### **الجداول الرئيسية**
- **users**: المستخدمون (عملاء، تجار، سائقين، إدمن)
- **stores**: المتاجر ومعلوماتها
- **products**: المنتجات والفئات
- **orders**: الطلبات وتفاصيلها
- **order_items**: عناصر الطلب
- **delivery_drivers**: سائقي التوصيل
- **categories**: فئات المنتجات
- **settings**: إعدادات النظام

#### **العلاقات**
```php
// مثال على العلاقات
class Order extends Model
{
    public function user(): BelongsTo
    {
        return $this->belongsTo(User::class);
    }
    
    public function store(): BelongsTo
    {
        return $this->belongsTo(Store::class);
    }
    
    public function deliveryDriver(): BelongsTo
    {
        return $this->belongsTo(DeliveryDriver::class);
    }
    
    public function orderItems(): HasMany
    {
        return $this->hasMany(OrderItem::class);
    }
}
```

---

## 🔐 الأمان والمصادقة

### 📱 نظام التحقق من الهاتف
- **OTP**: رمز التحقق المرسل عبر SMS
- **مدة الصلاحية**: 10 دقائق
- **إعادة الإرسال**: متاحة مع قيود زمنية
- **تشفير**: تشفير آمن للرموز

### 🛡️ الحماية
- **CSRF Protection**: حماية من هجمات CSRF
- **Rate Limiting**: تحديد معدل الطلبات
- **Input Validation**: التحقق من البيانات المدخلة
- **SQL Injection Protection**: حماية من هجمات SQL

---

## 📈 إمكانيات التطوير

### 🚀 ميزات مستقبلية
- **تطبيق جوال**: تطبيقات iOS وAndroid
- **ذكاء اصطناعي**: توصيات منتجات ذكية
- **تتبع GPS**: تتبع دقيق للمواقع
- **دفع إلكتروني**: تكامل مع بوابات الدفع
- **برنامج الولاء**: نقاط ومكافآت العملاء

### 🔧 تحسينات تقنية
- **تحسين الأداء**: cache وتحسين الاستعلامات
- **اختبارات شاملة**: Unit وIntegration tests
- **CI/CD**: خط إنتاج متكامل
- **مراقبة الأداء**: مراقبة النظام والأخطاء

---

## 📞 الدعم والمساعدة

### 🆘 قنوات الدعم
- **صفحة المساعدة**: إجابات على الأسئلة الشائعة
- **الدعم الفني**: فريق دعم متخصص
- **التواصل**: قنوات اتصال متعددة
- **التوثيق**: دليل شامل للمستخدمين

### 📚 الموارد
- **دليل المطور**: توثيق تقني شامل
- **API Documentation**: وثائق واجهة البرمجة
- **أمثلة التكامل**: أمثلة عملية للتطوير
- **أفضل الممارسات**: نصائح لتطوير أفضل

---

## 🎯 الخلاصة

**Getir Clone** هو نظام متكامل ومتطور لتوصيل البقالة والمنتجات الأساسية، يوفر:

✅ **تجربة عملاء ممتازة** مع واجهة سهلة الاستخدام  
✅ **نظام دفع متقدم** يدعم جميع طرق الدفع  
✅ **توصيل سريع وموثوق** في 10 دقائق  
✅ **إدارة شاملة** لجميع جوانب العمل  
✅ **أمان وحماية متقدمة** للبيانات والمعاملات  
✅ **قابلية التوسع** والتطوير المستمر  

هذا المشروع يقدم حلاً شاملاً ومتكاملاً لسوق التوصيل السريع، مع إمكانيات كبيرة للنمو والتوسع في المستقبل.
