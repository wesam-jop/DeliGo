# كيفية تشغيل الـ Server بشكل صحيح

## ⚠️ المشكلة:
إذا كان الـ server يعمل على `127.0.0.1:8000` فقط، فلن يقبل الطلبات من الأجهزة الأخرى على الشبكة.

## ✅ الحل:

### الطريقة 1: استخدام الأمر مباشرة
```bash
cd web
php artisan serve --host=0.0.0.0 --port=8000
```

### الطريقة 2: استخدام ملف PowerShell
```powershell
cd web
.\start-server.ps1
```

### الطريقة 3: استخدام ملف Batch
```cmd
cd web
start-server.bat
```

## 🔍 التحقق من أن الـ Server يعمل بشكل صحيح:

بعد تشغيل الـ server، افتح terminal جديد واكتب:
```bash
netstat -ano | findstr :8000
```

يجب أن ترى:
```
TCP    0.0.0.0:8000         0.0.0.0:0              LISTENING
```

**مهم:** يجب أن يكون `0.0.0.0:8000` وليس `127.0.0.1:8000`

## 📱 اختبار من الجوال:

افتح المتصفح على الجوال واذهب إلى:
```
http://192.168.1.100:8000/api/v1/stores
```

إذا رأيت JSON response، فالإعدادات صحيحة! ✅

## 🔥 إذا استمرت المشكلة:

1. **تحقق من الـ Firewall:**
   - افتح Windows Defender Firewall
   - أضف exception للمنفذ 8000

2. **تحقق من IP address:**
   ```bash
   ipconfig
   ```
   تأكد من أن IP address هو `192.168.1.100`

3. **تأكد من أنك على نفس الشبكة:**
   - الهاتف والكمبيوتر يجب أن يكونا على نفس Wi-Fi

